"""
domain.errors - placeholder (ph-00, Foundation).

Filled in by the phase that first needs concrete contracts here (see
domain/__init__.py for the mapping). Deliberately empty for now rather than
guessing the shape ahead of the phase that owns it.
"""
